补充以下优化项：

一、界面与交互

支持缩放比例（90%/100%/125%/150%）
支持浅色/深色/跟随系统主题
便签颜色模板与自定义配色
便签置顶开关（单个便签/全局）
拖拽吸附屏幕边缘与对齐辅助线
多显示器位置记忆（拔插显示器后自动纠偏）
快捷键总览面板（可搜索）
右键菜单分组优化（常用操作置顶）
最近使用功能（MRU）入口
首次使用引导与空状态提示

二、内容编辑体验
自动保存间隔可配置（如 5s/10s/30s）
撤销/重做栈增强（跨会话可选）
文本查找替换（当前便签/全部便签）
Markdown 基础支持（标题、列表、代码块）
超链接自动识别与一键打开
字体、行高、内边距可调
长文本折叠与展开
粘贴智能清洗（去除网页样式）

三、组织与管理
标签系统（工作/学习/临时等）
便签分组与批量操作（移动、删除、改色）
收藏/置顶便签区
归档箱与回收站（支持恢复）
全局搜索（支持标题、内容、标签）
最近编辑时间排序/创建时间排序
重复内容检测与合并建议

四、性能与稳定性
大量便签下的懒加载与虚拟化
启动加速（延迟加载非核心模块）
崩溃自动恢复（启动时提示恢复会话）
数据写入去抖（减少频繁磁盘 I/O）
异常日志分级与一键导出
内存占用监控与告警阈值

五、数据安全与备份
本地自动备份（按天/按版本保留）
手动导出导入（JSON/纯文本）
关键数据加密存储（可设置主密码）
删除二次确认与可撤销删除
数据完整性校验（损坏检测与修复提示）

六、系统集成
开机自启动与启动后最小化到托盘
托盘快捷操作（新建便签、显示/隐藏全部）
全局快捷键快速新建便签
与系统通知集成（提醒到期提示）
截图粘贴识别（OCR 可选）

七、可配置性与可访问性
设置项搜索框（快速定位配置）
设置页按“基础/高级”分层
高对比度模式与色弱友好配色
键盘无障碍全操作路径
字号随系统 DPI 自动适配

八、可运维与版本体验
检查更新与更新日志弹窗
配置重置为默认值
诊断信息一键复制（版本、系统、日志路径）
问题反馈入口（自动附带诊断信息）

---

执行进展（2026-04-15）

已完成（本轮聚焦：一、界面与交互）
- 便签颜色模板与自定义配色：已支持预设色、自定义颜色选择。
- 最近使用色：已记录并展示最近颜色（去重、数量限制、合法性校验）。
- 一键恢复默认颜色：已提供“恢复默认纸张颜色”。
- 拖拽吸附屏幕边缘：已实现窗口贴边吸附。
- 对齐辅助线：已在吸附时绘制边缘辅助线提示。
- Shift 临时关闭吸附：拖拽过程中按住 Shift 可跳过吸附。
- 吸附阈值可配置：设置页新增阈值项并持久化。
- 右键菜单分组优化：已按“常用/外观/内容/工具”分组并将高频项前置。
- 最近使用功能（MRU）入口：已支持最近动作列表与“重复上次操作”。
- 首次使用引导与空状态提示：已支持首次提示且可关闭后不重复打扰。

验证结果
- 已完成构建验证：`dotnet build StickyNote.slnx`。
- 构建结果：0 Warning，0 Error。